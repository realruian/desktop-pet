# ── 依赖与构建产物 ────────────────────────────────────────────
# （build/ 不忽略：那里放打包资源 icon.icns / extend.plist / make-icon.py）
node_modules/
dist/
out/
build/icon.iconset/

# ── 本地杂项备份 ─────────────────────────────────────────────
_asset_repair_backup_*/
_qa_contact_sheets/

# ── PyInstaller / 像素动画生成实验的运行输出（体积大、与本项目无关）──
*_run/
mochi_pet_run/
mochi_pixel_pet_run/
pixel_girl_pet_run/
make_mochi_pet.py
*.spec

# ── Python 缓存与虚拟环境 ─────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/

# ── 原始动画帧 dump（仅作素材留档；App 实际加载的是 assets/）──────
像素狗狗动作帧/

# ── 环境变量与密钥 ────────────────────────────────────────────
.env
.env.*
*.pem
*.key
# 本地配置（含 Kimi API Key）——只提交 config.example.json
config.json

# ── 日志与系统文件 ────────────────────────────────────────────
*.log
.DS_Store

# 根目录零散导出帧（素材草稿，不入库）
/frame_05*.png
